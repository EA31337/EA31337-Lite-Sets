language: python
python: '3.4'

sudo: required

addons:
  hosts:
    - travis.local

env:
  global:
    - CWD="$TRAVIS_BUILD_DIR"

cache:
  - apt
  - pip
  directories:
    - /opt
    - ~/.wine

before_install:
  - env

install:
  - env

before_script:
  - env

script:
  - env
